@using (Html.BeginForm(null, null, new { @returnUrl = Model.ReturnUrl}, FormMethod.Post, new { data_eml_editconfirmation = "true" }))
{
    @Html.AntiForgeryToken()

    <div class="modal-body">
        <div class="form-horizontal" id="LayoutCreateEditContent">
            @Html.Partial(Model.ContentViewModel.LayoutEditContentName as string)
        </div>
    </div>
    <div class="modal-footer">
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <span id="busyindicatorEdit" style="display: none;">
                    @Html.Partial("_BusyIndicatorSmall")
                </span>
                
                @Html.Partial("_EditButton")
            </div>
        </div>
    </div>
}

